# WAV CNN 模型来源

本目录只保留应用运行必需的默认推理资产：

- `best_model_cnn.pt`
- `mean_std_new.pkl`

它们来自 `https://github.com/suahi/Voice_detection_test` 的提交 `b5c73ea`。模型定义与特征提取实现位于 `src/voice_fault_diagnosis/inference/wav_cnn.py`，并保持与该仓库 `inference.py` 的默认路径兼容。
